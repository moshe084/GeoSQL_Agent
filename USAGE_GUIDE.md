# 📘 מדריך שימוש - Geo-SQL Agent

> **מערכת AI שמתרגמת שאלות בעברית/אנגלית לשאילתות SQL מרחביות**

---

## 🎯 מה זה עושה?

במקום לכתוב קוד SQL מסובך, אתה פשוט שואל בשפה טבעית:
- **"הצג את כל בתי הקפה"** → המערכת יוצרת SQL ומציגה על מפה
- **"מצא פארקים במרחק 200 מטר מכביש דיזנגוף"** → המערכת מבינה ומחפשה

---

## 🏗️ מבנה המערכת (פשוט)

```
אתה שואל שאלה ↓
┌─────────────────────────────┐
│  🌐 Frontend (דפדפן)         │  ← פה אתה מקליד שאלות ורואה מפה
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  🤖 Backend (שרת Python)     │  ← פה GPT-4 הופך שאלה ל-SQL
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  🗄️ PostGIS (מסד נתונים)     │  ← פה כל המידע הגיאוגרפי
└─────────────────────────────┘
```

---

## 📦 התקנה (5 דקות)

### דרישות מקדימות:
1. **Docker Desktop** - [הורד כאן](https://www.docker.com/products/docker-desktop/)
2. **OpenAI API Key** - [קבל כאן](https://platform.openai.com/api-keys) (בערך $0.01 לשאלה)

### שלבי ההתקנה:

#### 1️⃣ הורד את הקוד
```bash
git clone https://github.com/moshe084/MasterRepo.git
cd MasterRepo
```

#### 2️⃣ הגדר את ה-API Key
צור קובץ `.env` בתיקיית הפרויקט:

**Windows:**
```bash
echo OPENAI_API_KEY=sk-your-actual-key-here > .env
```

**Mac/Linux:**
```bash
echo "OPENAI_API_KEY=sk-your-actual-key-here" > .env
```

**או פשוט:** העתק את הקובץ `.env.example` לקובץ `.env` ותשנה את המפתח.

#### 3️⃣ הרץ את המערכת
```bash
docker-compose up -d
```

זהו! המערכת רצה ב-3 קונטיינרים:
- ✅ Frontend (http://localhost:3010)
- ✅ Backend (http://localhost:8000)
- ✅ PostGIS Database (localhost:5433)

---

## 🚀 שימוש ראשון

### פתח את הדפדפן:
```
http://localhost:3010
```

### שאל שאלה פשוטה:
```
Show me all cafes
```

או בעברית:
```
הצג את כל בתי הקפה
```

### תראה 3 דברים:
1. **SQL Query** - השאילתה שהמערכת יצרה
2. **מפה** - נקודות שמציגות את התוצאות
3. **סטטיסטיקה** - כמה תוצאות מצאנו ובכמה זמן

---

## 💡 דוגמאות לשאלות

### 🟢 קל (למתחילים)

| שאלה | מה זה עושה |
|------|-----------|
| **Show me all cafes** | מציג את כל בתי הקפה על המפה |
| **Show all parks** | מציג את כל הפארקים |
| **הצג פארקים גדולים מ-5000 מטר רבוע** | סינון לפי גודל |

### 🟡 בינוני (מרחבי)

| שאלה | מה זה עושה |
|------|-----------|
| **Find cafes within 200 meters of the largest park** | מציאת בתי קפה ליד הפארק הגדול |
| **What is the closest cafe to the smallest park?** | בית הקפה הכי קרוב לפארק הקטן |
| **מצא בתי קפה במרחק 50 מטר מכבישים ראשיים** | קרבה לכבישים |

### 🔴 מתקדם (ניתוח)

| שאלה | מה זה עושה |
|------|-----------|
| **Which park has the most cafes within 300 meters?** | ניתוח צפיפות |
| **Show roads that intersect with parks** | חיתוכים גיאומטריים |
| **Count cafes within 500m of each park** | ספירה לפי אזור |

---

## 🗺️ מה יש במסד הנתונים?

המערכת מכילה דאטה של **תל אביב**:

| טבלה | תיאור | כמות | דוגמה |
|------|-------|------|-------|
| **cafes** | בתי קפה | ~15 | Aroma, Landwer, Arcaffe |
| **parks** | פארקים וגנים | ~7 | פארק הירקון, גן מאיר |
| **roads** | רחובות | ~6 | דיזנגוף, רוטשילד |

---

## 🔍 איך לראות מה קורה מאחורי הקלעים?

### 🖥️ לוגים של השרת (Backend):
פתח טרמינל נוסף והרץ:
```bash
docker-compose logs -f backend
```

תראה:
```
🔵 NEW QUERY REQUEST
📝 Question: Show me all cafes
🤖 STEP 1: Sending to OpenAI...
✅ Received SQL: SELECT id, name...
🗄️ STEP 2: Executing on database...
✅ Query executed successfully
🟢 COMPLETED - 15 results in 1.2s
```

### 🌐 לוגים של הדפדפן (Frontend):
1. פתח **Developer Tools** (F12)
2. לך לטאב **Console**
3. תראה את כל השלבים בזמן אמת

---

## 🛠️ פקודות שימושיות

### להפעיל את המערכת:
```bash
docker-compose up -d
```

### לעצור את המערכת:
```bash
docker-compose down
```

### לראות לוגים:
```bash
docker-compose logs -f          # כל השירותים
docker-compose logs -f backend  # רק השרת
docker-compose logs -f postgis  # רק מסד הנתונים
```

### לבדוק שהכל רץ:
```bash
docker-compose ps
```

תראה:
```
NAME              STATUS
geo-sql-backend   Up
geo-sql-frontend  Up
geo-sql-postgis   Up (healthy)
```

### לבנות מחדש (אחרי שינויים בקוד):
```bash
docker-compose up --build -d
```

---

## 🐛 פתרון בעיות נפוצות

### ❌ בעיה: "Port 5432 already allocated"
**פתרון:** יש לך PostgreSQL מקומי שרץ.

שנה את הפורט ב-`docker-compose.yml`:
```yaml
ports:
  - "5433:5432"  # במקום 5432:5432
```

---

### ❌ בעיה: "OpenAI API key not found"
**פתרון:** לא יצרת קובץ `.env`.

```bash
# צור קובץ .env עם המפתח שלך:
echo OPENAI_API_KEY=sk-your-key-here > .env

# הפעל מחדש:
docker-compose restart backend
```

---

### ❌ בעיה: "resultsLayer.getBounds is not a function"
**פתרון:** גרסה ישנה של הקוד.

**כבר תוקן!** רענן את הדפדפן עם **Ctrl+Shift+R**

---

### ❌ בעיה: "Query execution failed: Sorry, but the database..."
**פתרון:** שאלת שאלה על משהו שלא במסד הנתונים.

המערכת יודעת רק על:
- ✅ cafes (בתי קפה)
- ✅ parks (פארקים)
- ✅ roads (כבישים)

**לא עובד:**
- ❌ "Find ports" (אין נמלים)
- ❌ "Show hospitals" (אין בתי חולים)

---

### ❌ בעיה: המפה לא טוענת
**פתרון:** בדוק את ה-Console בדפדפן.

```bash
# בדוק שהשרת רץ:
curl http://localhost:8000/health

# אם מקבל: {"status":"healthy","database":"connected"}
# אז הבעיה בדפדפן - נקה cache
```

---

## 📊 איך זה עובד? (הסבר טכני)

### תהליך מלא של שאילתה:

```
1. אתה: "Find cafes near parks"
   ↓
2. Frontend: שולח POST ל-http://localhost:8000/query
   ↓
3. Backend: שולח לOpenAI GPT-4 עם System Prompt:
   "You are a PostGIS expert. Database has: cafes, parks, roads..."
   ↓
4. GPT-4: מחזיר SQL:
   "SELECT c.id, c.name, ST_AsGeoJSON(c.geom) as geojson
    FROM cafes c, parks p
    WHERE ST_DWithin(c.geom::geography, p.geom::geography, 200)"
   ↓
5. Backend: מריץ את ה-SQL על PostGIS
   ↓
6. PostGIS: מחזיר שורות עם GeoJSON:
   [{id: 1, name: "Aroma", geojson: {...}}, ...]
   ↓
7. Backend: שולח את התוצאות ל-Frontend
   ↓
8. Frontend: מוסיף נקודות למפה Leaflet
   ↓
9. אתה רואה: מפה עם סמנים + SQL + סטטיסטיקה
```

---

## 🎓 למה זה שימושי?

### למפתחים:
- ✅ ללמוד PostGIS בלי לזכור syntax
- ✅ לבדוק שאילתות מהר
- ✅ להבין איך AI יוצר קוד

### למנהלי נתונים:
- ✅ לחקור דאטה גיאוגרפי בלי SQL
- ✅ לייצר דוחות מרחביים
- ✅ לזהות דפוסים גיאוגרפיים

### לסטודנטים:
- ✅ פרויקט להציג בקורות חיים
- ✅ דוגמה לשילוב AI + GIS
- ✅ למידה של Docker + FastAPI + PostGIS

---

## 🔐 אבטחה - חשוב לדעת!

⚠️ **זו גרסת POC (Proof of Concept) - לא לייצור!**

**בעיות אבטחה:**
- ❌ אין אימות משתמשים
- ❌ אין הגנה מ-SQL Injection
- ❌ CORS פתוח לכולם
- ❌ API Key בקובץ `.env`

**לפני production:**
1. הוסף Authentication (JWT tokens)
2. הוסף Rate Limiting
3. בדוק SQL לפני ריצה
4. העבר secrets ל-AWS Secrets Manager

---

## 📝 הרחבת המערכת

### איך להוסיף טבלה חדשה?

**1. הוסף SQL ב-`init-data/02-load-sample-data.sql`:**
```sql
CREATE TABLE restaurants (
    id SERIAL PRIMARY KEY,
    name TEXT,
    geom GEOMETRY(Point, 4326)
);

INSERT INTO restaurants (name, geom) VALUES
('Shawarma King', ST_SetSRID(ST_MakePoint(34.7818, 32.0853), 4326));
```

**2. עדכן את ה-System Prompt ב-`backend/main.py`:**
```python
SYSTEM_PROMPT = """
...
3. Table: restaurants
   - id (integer)
   - name (text)
   - geom (geometry, Point)
...
"""
```

**3. הפעל מחדש:**
```bash
docker-compose down -v  # מוחק volumes
docker-compose up --build -d
```

---

## 🎬 מוכן לשימוש!

### צ'קליסט אחרון:
- [ ] Docker Desktop רץ
- [ ] יצרת קובץ `.env` עם OpenAI API Key
- [ ] הרצת `docker-compose up -d`
- [ ] פתחת http://localhost:3010
- [ ] ראית 3 קונטיינרים UP ב-`docker-compose ps`

**אם הכל ✅ - תתחיל לשאול שאלות!**

---

## 🆘 קיבלת שגיאה?

1. **בדוק לוגים:**
   ```bash
   docker-compose logs -f backend
   ```

2. **בדוק שהשרת רץ:**
   ```bash
   curl http://localhost:8000/health
   ```

3. **פתח Console בדפדפן (F12)** וחפש שגיאות

4. **הפעל מחדש הכל:**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

---

## 🎯 דוגמה מלאה (צעד אחר צעד)

### שאלה: "Find cafes within 200m of the largest park"

**1. מה אתה רואה בדפדפן:**
```
SQL Generated:
SELECT c.id, c.name, ST_AsGeoJSON(c.geom) as geojson
FROM cafes c, parks p
WHERE p.area = (SELECT MAX(area) FROM parks)
AND ST_DWithin(c.geom::geography, p.geom::geography, 200);

Results: 3 cafes found
Execution time: 1.2s
```

**2. מה המערכת עשתה:**
- 🤖 GPT-4 הבין שאתה מחפש בתי קפה ליד הפארק הגדול
- 📐 יצר SQL עם `MAX(area)` למציאת הפארק הגדול
- 📏 השתמש ב-`ST_DWithin` למרחק 200 מטר
- 🌍 השתמש ב-`::geography` למדידה במטרים
- 🗺️ המיר ל-GeoJSON עם `ST_AsGeoJSON`

**3. התוצאה על המפה:**
- סמנים כחולים על 3 בתי קפה
- המפה התאימה את עצמה לגבולות התוצאות
- לחיצה על סמן מראה פרטים

---

## 📚 למידה נוספת

- [PostGIS Functions](https://postgis.net/docs/reference.html)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Leaflet Map Tutorial](https://leafletjs.com/examples.html)
- [OpenAI API Guide](https://platform.openai.com/docs)

---

## 💬 שאלות נפוצות (FAQ)

**Q: כמה עולה להריץ את זה?**
A: כ-$0.01 לשאלה (GPT-4 API). Docker חינם.

**Q: אפשר להשתמש בעברית?**
A: כן! GPT-4 מבין עברית מצוין.

**Q: אפשר להוסיף דאטה אמיתית?**
A: כן! ערוך את `init-data/02-load-sample-data.sql`

**Q: למה הפורט 5433 ולא 5432?**
A: כדי לא להתנגש עם PostgreSQL מקומי.

**Q: אפשר להשתמש בגרסה חינמית של OpenAI?**
A: לא, צריך API key בתשלום. אבל זול מאוד.

---

**🎉 בהצלחה! תהנה מהמערכת!**

---

**📧 נתקלת בבעיה?** פתח issue ב-GitHub: https://github.com/moshe084/MasterRepo/issues
